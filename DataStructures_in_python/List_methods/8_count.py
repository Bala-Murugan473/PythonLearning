# returns number of time the given element is present in the list

l = [1,1,2,3,4]
n_times = l.count(1)
print(n_times)

# if the element is not present in the list, it returns 0
n_times = l.count(100)
print(n_times)