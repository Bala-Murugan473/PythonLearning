# similar to list but immutable.

# FEATURES OF TUPLE:
# Ordered
# Immutable
# Allow duplicates
# work in index basis
# Slicable
# can contain any datatype

t = (1, 2, 3, 4)

# NOTE: use a trailing comma, when the tuple contains only one item.( singleton tuple)

t1 = (1,)

# Note : comma that actually makes tuple not the parenthesis

tup = 1, 2, 3
print(type(tup))
